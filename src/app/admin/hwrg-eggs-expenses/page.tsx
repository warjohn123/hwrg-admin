import ExpensesTable from '@/components/ExpensesTable';
import { IAssignment } from '@/types/User';

export default function HWRGEggsExpensesPage() {
  return <ExpensesTable type={IAssignment.HWRG_EGGS} />;
}
